import { Request, Response } from "express";
declare class BotsController {
    getMove(req: Request, res: Response): Promise<void>;
}
declare const _default: BotsController;
export default _default;
